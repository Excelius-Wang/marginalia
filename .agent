# Marginalia 论文精读 Agent 工作流

使用 `marginalia` skill 时，**正式产物是飞书文档**（飞书 Wiki 知识库 + Base 索引）；本地 md 与映射只是 gitignore 的运行期缓存。

1. 按 skill 解析来源、判定领域、深度阅读。开始前先查飞书 Base 索引（或本地 `notes/` 缓存）有没有同一篇，避免重复建文档。
2. 产出或更新 `notes/<领域>/` 下的一篇笔记（本地运行期缓存，gitignore，不提交）。需遵循当前模板，并写好招牌段落：`毒舌评论`、`作者思路重建`、`最脆弱的假设`、`最小复现实验`、`最强反例设计`、`Follow-up Research Idea`。
3. 发布到飞书（见 `skills/marginalia/references/feishu-publish.md`）：跑 `publish_to_feishu.py` 生成飞书文档、挂进 Wiki 领域节点、登记多维表格索引，并把 Feishu URL 写回本地笔记。幂等且原地更新（URL 不变，依赖本地 `.marginalia/feishu.json` 映射）。**这一步就是知识库的落地——飞书是索引，不回填 README。**
4. **日常精读发表论文不产生 git 提交**：`notes/`、`.marginalia/feishu.json`、`.marginalia/figs/` 都已 gitignore。
5. 只有当本次**改了 skill 本体**（`skills/marginalia/` 脚本/references/SKILL、`README*`、`.agent`、`.gitignore`）时才提交：`git status --short` → 只 stage 本次有意改动的 skill 文件 → commit（信息描述这次 skill 改动）→ `git push`。无改动不空 commit；绝不 stage 用户的无关改动或 gitignore 的运行期缓存。
